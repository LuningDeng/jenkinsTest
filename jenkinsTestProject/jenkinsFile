pipeline {
    agent any 
    tools { 
        maven 'M3' 
        jdk 'JDK1.8' 
    }
    stages {
        stage ('Initialize') {
            steps {
 					echo 'This is a minimal pipeline.'
            }
        }
    }
}